# LegalTech AI MCP Server — Runtime requirements
# Install: pip install -r requirements.txt
# Then: python -m spacy download en_core_web_sm

# API server
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# File parsing
pdfplumber==0.11.0
python-docx==1.1.2

# NLP / ML used by API pipeline
transformers==4.41.2
torch==2.3.0
sentence-transformers==3.0.1
spacy>=3.7.4

# Stability pins for Windows + spaCy/chromadb compatibility
numpy==1.26.4
onnxruntime==1.18.1

# Retrieval store used by RAG endpoint
chromadb==0.5.3

# LLM backends (OpenAI/OpenRouter via openai SDK, or Ollama via requests)
openai==1.30.1
requests==2.31.0
python-dotenv==1.0.1

# Testing framework (no LLM dependencies)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
